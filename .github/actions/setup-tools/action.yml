name: Setup Tools
description: Setup required tools in the workspace

runs:
  using: composite
  steps:
  - name: Setup Tools
    uses: egose/actions/asdf-tools@7a010fcf15d592c751df049c071db109b3e4e6eb
    with:
      plugins: |
        picotools=https://github.com/egose/picotools.git

  - name: Install python tools
    run: |
      pip install -r requirements.txt
      asdf reshim
    shell: bash
